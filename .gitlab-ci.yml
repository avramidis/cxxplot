stages:
  - build
  - test

build:ubuntu:
  image: ubuntu:focal
  stage: build
  tags:
    - docker
  script:
    - apt-get update && apt-get install -y build-essential wget cmake
    - cd /builds/$CI_PROJECT_PATH
    - mkdir build
    - cd build
    - cmake ..
    - make
  artifacts:
    paths:
      - build